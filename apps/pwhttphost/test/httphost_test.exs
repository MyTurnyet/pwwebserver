defmodule PW.HTTPHost_Test do
  require Logger
  use ExUnit.Case

  # describe "pwhttphost tests" do
  #   test "accept/1 should return status of :ok" do
  #     {status, _} = PW.HttpHost.listen()
  #     assert status == :ok
  #   end

  #   test "accept/1 should return a valid port" do
  #     {status, port} = PW.HttpHost.listen()
  #     assert status == :ok
  #     assert is_port(port)
  #   end

  #   test "accept/1 should accept a parameter for port" do
  #     {:ok, port} = PW.HttpHost.listen(7777)
  #     assert is_port(port)
  #   end
  # end
end
